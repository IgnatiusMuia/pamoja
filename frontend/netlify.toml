[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"

# Pure static export (next.config: output = "export"), no server runtime.
# API calls go straight to NEXT_PUBLIC_API_URL (backend on Render).