[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Rewrites for SPA-style public paths are handled by the Next.js runtime plugin;
# API calls go straight to NEXT_PUBLIC_API_URL (backend on Render/Railway).